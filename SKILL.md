---
name: product-definition
description: 产品定义一体机——从"该不该做"到"生成完整蓝图"到"质量门评审"。当用户涉及产品/系统立项的完整流程时触发:①该不该做/自研 vs 现成/已有XX还自研吗/要不要立项(走存在性关)②生成用户画像/痛点/业务架构/用户旅程/产品架构/商业模式画布/指标树(走蓝图生成)③评审这份蓝图/找硬伤/看看定稿行不行(走质量门)。三者是"生成→审查→再生成"的连续闭环,一个入口自动分流。
user-invocable: true
version: 0.1.0
---

# Product Definition · 产品定义一体机(存在性闸 → 蓝图生成[内嵌质量门] → 定稿)

把"该不该做、怎么做、做得好不好"合成一条流水线。核心信条——**先过存在性闸(该不该),再造蓝图(怎么做),每一步到达里程碑立即走质量门(review),不合格回去改,循环到定稿。**

> 本 skill 由 `product-ideation-review`(审)与 `product-blueprint-writer`(造)合并而来。合并原因:二者在真实使用中是**严格串行的连续闭环**("造→审→改→再审"),在一个项目/会话连续进行,单独成两个要来回切。合并后 review 内嵌为生成流程的质量门,不用切换。
> 反例素材:项目内 `docs/立项复盘-自研CRM反例-2026-09-24.md`(该产品全套产物是被否产物,不得当成功模板;可复用的是顺序 + "每步死在哪个门"的提示)。
> memory 参照:`crm-ideation-lesson`(先盘存量)、`crm-calibration-lesson`(数字校准)。

## 入口分流(你说什么,走到哪)

| 你说 | 走哪 |
|------|------|
| "值不值得做 / 自研还是买 / 已有XX系统还自研吗 / 要不要立项" | **存在性闸**(第 1 章)|
| "生成用户画像 / 痛点 / 业务/产品架构 / 画布 / 旅程 / 指标树" | **蓝图生成**(第 2 章)|
| "评审这份蓝图 / 找硬伤 / 看看定稿行不行" | **质量门**(第 2 章内嵌)|

## 铁律(通用,贯穿全流程)

1. **存在性闸前置,不可跳。** 任何"做 X 值不值得"先盘——已有的系统/商业软件能否覆盖?翻车死法:全程没问"已有销氪CRM"。已付费存量系统 → 再自研 = 重复掏钱(见 crm-ideation-lesson)。
2. **用现成理由只三种**①一般覆盖不了②现成贵到不划算③有独有数据/壁垒。
3. **增量对照"现状全部能力(含现成已给)"算,不从零基线。**
4. **诊断 ≠ 价值。** 痛点要落到"可量化、可归因"(续约率救回几点×户均年费)。"看不清全貌/提效"是诊断不是价值。
5. **基线靠真实事实,不给报告/估算。** 一驳就穿(crm-calibration-lesson)。
6. **作业 vs 经营层先划**,MECE 每层一维度,横切关注点进流程图不进结构图。
7. **MVP 第一刀是真闭环 + 有责任主体**,不是假控件、不是"拉着好看"功能(全貌/标签/首页)。
8. **被否时分辨"否方案 vs 否存在性"**,需求未必被否,入口可改"现成+增量"。

## 第 1 章 · 存在性闸(该不该做 / 用现成还是自研)

> 过后面的蓝图生成才不空转。用户用真实事实答,不推理代填。

| # | 问题 | 若"否/不清" | 来源 |
|---|------|------------|------|
| 1.1 | 已有系统/商业软件能否覆盖?(盘点全部存量+SaaS,不止同类) | 反方直接否,先查现成能给多少 | 销氪被漏 |
| 1.2 | 覆盖不了的是什么独特场景?(具体非"更适合我们") | 现成够,自研缺立足 | 覆盖不了① |
| 1.3 | 现成贵到不划算?(license vs 自研人力) | 现成划算,自研性价比负 | 不划算② |
| 1.4 | 有没有独有数据/壁垒?(别人拿不到的数据集) | 无壁垒=普通工具拼价 | 有壁垒③ |
| 1.5 | 增量基准(从"现状含现成已给"算,非从零) | 基错→数字虚 | 增量对照 |
| 1.6 | 激励/考核匹配价值?(无激励→空转) | 没人愿用/无人负责→坟场 | 续约无责任人 |

**任一红灯 → 停下,转"现成+增量 / 暂缓"。** 全绿进第 2 章。

## 第 2 章 · 蓝图生成(七件套,内嵌质量门)

> 顺序不可跳。每到达里程碑立即走**质量门**(第 3 章),不过 → 回对应步改 → 再审(循环)。

```
第0步 存在性闸(已过,见第1章)
  ↓
第1步 用户画像 ──→ 【质量门】角色≤5/痛可证伪/区分P与次要
  ↓
第2步 痛点+基线 ──→ 【质量门】可量化可归因/数字真实非估算
  ↓
第3步 业务架构 ──→ 【质量门】作业vs经营层边界/现成覆盖记录
  ↓
第4步 用户旅程 ──→ 【质量门】与画像/架构自洽/横切不进结构
  ↓
第5步 产品架构 ──→ 【质量门】资产/能力分层/现成是否重复/"拉着好看"砍
  ↓
第6步 商业模式画布 ──→【质量门】内部工具=值不值非怎么赚
  ↓
第7步 指标树 ──→【质量门】北极星可测/先过程指标/有归因
  ↓
定稿
```

每步模板在 `templates/`(user-persona / pain-point / business-arch / journey / product-arch / canvas / metrics-tree)。每步的死因门提示:①画错对象空转 ②诊断当价值 ③边界混淆返工 ④自洽失败 ⑤重复现成白投入 ⑥内部工具偏营收 ⑦北极星不可证伪。

### 每步执行模式(四段,防"推理代填"与"问答疲劳")

每一件都在 **①计划 → ②交互收集 → ③产初稿 → ④验证确认** 四段里推进,不是直接甩模板填空。

1. **①计划**:先盘信息缺口——本步模板哪些字段已有答案(承接上一步产物 / 项目已有文档 / 用户已给过),哪些缺。缺的才进入下一步提问。
2. **②交互收集**:**只问缺的,不重问已有的**。给具体问题让用户答(参照铁律5:基线靠真实事实,不推理代填)。答不够不产稿。例如第1步用户画像,不是生成者凭空造角色,而是问"一线谁、管理者谁、各被什么考核、现用什么工具"。
3. **③产初稿**:按模板填。交互收集不到的标 `[PLACEHOLDER—NEEDS RESEARCH]`;推断/听说的标 `[ASSUMPTION—VALIDATE]`。禁止用默认可信包装未知。
4. **④验证确认**:先走本步**质量门(3.3 专项清单)**——机器审通过 ≠ 完事;再**交用户人工确认**,用户点头才算本步定稿。must-fix 回改再审,直到机器与人双过,才进下一步。

四段与 proto→validated 标签联动:交互收集不到 → PLACEHOLDER;推断 → ASSUMPTION;用户双过 → 升 VALIDATED。**agents 产信息、人产决策**(项目规矩),初稿可由主线程/agent 产出,但"能不能定稿"只能用户拍板。

## 第 3 章 · 质量门(蓝图的评审,内嵌于第 2 章)

> 每步到达里程碑**立即**执行,不是最后一次性。产出=**通过 / 不通过 + 具体 must-fix**,每一条 must-fix 必须带"改哪个里程碑、怎么验证已修复"。

### 3.1 评审流程(五步)

1. **对照里程碑产物**:拉起被审的那一步产物(来自 `templates/` 对应模板的已填内容)。
2. **逐条过审查清单**:用下方对应里程碑的 checklist,逐项判 ✅(达标)/ ❌(不达标)/ ⚠️(存疑)。
3. **汇出判定**:任一 ❌ → 不通过;只有 ⚠️ 或全 ✅ → 通过。
4. **给 must-fix**:每条 ❌/⚠️ 写"问题 → 该回第几步改 → 用什么证据验证已修复"。
5. **回改再审**:不通过则回对应步骤改,再回到本门复审,直到通过。

**判定证据 = 蓝图里的实际内容/来源,不是"我觉得可以"。** 每说"通过",要能指着对应产出的某一行说"这条满足检查 2.3 因为……"。

### 3.2 通用审查清单(全品类都查)

- **价值可量化可归因**:痛点落到可算钱、可证明是系统带来的;诊断(看不清全貌/提效)不是价值。
- **真闭环 + 责任主体**:第一刀进入→操作→状态变更→反馈→下一步→可返回,缺一环未完成;责任主体明确且可能被问责。
- **MECE / 边界**:每层一个维度;资产(KR)与能力(KA)分层;作业 vs 经营层划清;横切关注点进流程图不进结构图。
- **未来买单≠现在该建**:跨线/多业务布局理由,只在该布局确定性高时才成立,否则是给不确定未来提前付钱。
- **假设标"待验证" + 给验证出口**:不写死端到端承诺。**逐字段/逐格标注,不整页一个帽子**——用 proto→validated 轨道(PROTO 假设态/VALIDATED 核实态)+ `[ASSUMPTION—VALIDATE]` / `[PLACEHOLDER—NEEDS RESEARCH]` 标签(各模板已内置小节/列)。"听说的"覆盖判断未经查证不得当结论。

### 3.3 里程碑专项审查清单(★review 的核心)

> 每个里程碑走**对应的专项清单**(取代泛泛的通用条款)。以下每项都源于真实失败的教训。

**审用户画像(milestone:用户画像)**
- P1 角色≤5?区分了主(P)与次要?
- 每个痛点**可证伪、有验证方式**(访谈/数据)?还是口号式("难用""效率低")?
- 一线 vs 管理者痛点分清了?(一线求省事,管理者看结果)
- 现有工具(Excel/已有系统/销氪)记了吗?——**没记 = 下一轮"已有系统覆盖"才发现 = 返工**

**审痛点+基线(milestone:痛点清单/基线)**
- 每个痛点能落到**可量化的钱/时**?(续约率救回几点×户均年费)
- 基线数字**从真实资料拉得出来**,还是靠估?估算是否标了区间?(crm-calibration-lesson)
- 有对照/历史可回放证明"做了变好"吗?——没有 → 增量无法归因 → 老板一驳就穿

**审业务架构(milestone:业务架构)**
- 作业 vs 经营层边界划清了吗?跨线归因/客户360在经营层,记账作业归代账系统。
- **现有系统覆盖记录了吗**?(销氪/代账能给多少)——不记 = 自研重复建设。
- MECE:一层一个维度?横切关注点没被硬塞成平级节点?

**审用户旅程(milestone:用户旅程)**
- 每个环节对得上业务架构里的角色/边界?无矛盾?(自洽性)
- 横切关注点(异常/审批/日志/鉴权)进了流程图,不是平级环节(CLAUDE.md 规范)?
- 旅程覆盖了"维护/到期/续约"阶段,不是只到"使用"就停?(闭环后半段)

**审产品架构(milestone:产品架构)**
- 资产(KR)/能力(KA)分层?不混一层?
- 每功能过"现成有没有 / 属作业还是经营 / 是真闭环还是拉着好看"三问?
- "拉着好看"功能(全貌/标签/首页)立项期是负担,砍或降级了吗?

**审商业模式画布(milestone:商业画布)**
- 内部工具:重点是"值不值"(成本/产出)不是"怎么赚"?
- 值不值算了吗?投入(人力×时间)vs 产出(增量+提效)有数?
- 若要对外:收费模式/可复制/市场价值三问答得出?

**审指标树(milestone:指标树)**
- 北极星**可测/可归因**?基线立得住?(不可证伪的北极星 MVP 期别硬给)
- **先给了过程指标**(覆盖率/回写率),增量/结果指标有归因才承诺?
- 有反指标?(避免"为达标录假数""手工录入负担")

### 3.4 特殊复审机制

- **跨角色交叉校法**(可选):重要蓝图/关键决策用 `tri-model-review` 跨厂三角(技术Kimi/商业Codex/市场Claude)复核——三家独立建模,揪单模型盲区。**重量级,只在关键里程碑用,不每次跑。**
- **反例对照**:审任何步时,对照反例素材(项目内 `docs/立项复盘-自研CRM反例`)——"这一步会不会重演那个死因"。

## 定稿与落档

- 七件套产物写入 `docs/`(本项目约定:业务架构/产品架构/用户画像等),用各自 template。
- 关键决策/推敲记 `decisions/decision-log.md` + 推进位置看 `backlog/process.md`。
- 产出新教训 → memory 两处同步(`memory/` + `~/.claude`),格式见 `crm-ideation-lesson`。
- **(可选,看本仓库习惯)生成 HTML 可视化门户**:本项目习惯把定稿的架构/画像/旅程/指标树等生成 `docs/html/*.html` 单页视图(深色顶栏+导航,tab 切换),便于浏览器查看、分享。规则:①每个视图一个独立 `.html` 文件,索引页 `index.html` 卡片式导航聚合 ②**改 HTML 时必须同步改对应 md 源**,两手要一致(以 md 为母本,HTML 是渲染视图) ③交互类 HTML 必须过全局「HTML/前端交付验证规范」的 jsdom 运行时校验(见 ~/.claude/CLAUDE.md),不能只做静态配平检查。此步是展示/沟通习惯,非蓝图方法论本身,非必经步骤——不生成 HTML 不影响蓝图定稿。

## 关联

- 跨厂三角评审:`tri-model-review`(项目级,依赖 `call_kimi.py`)。
- 单文档规格评审:`prd-reviewer` / `review-requirements`。
- architecture MECE 规范:CLAUDE.md。
- 反例素材:`docs/立项复盘-自研CRM反例-2026-09-24.md`。