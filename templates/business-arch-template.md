# 业务架构模板

> 生成者:`product-blueprint-writer` 第 3 步
> 铁律:MECE(一层一个维度);作业 vs 经营层边界先划;顺手记录现有系统覆盖(避免自研重复)。

## 一、定位
- 产品一句话定位(服务谁 / 解决什么核心价值):

## 二、服务对象(承接用户画像)
| 角色 | 服务价值 | 考核指标 |
|------|---------|---------|
| | | |

## 三、业务边界(★核心)
> **判别开关**:内部系统 → 划"作业 vs 经营层";对外产品 → 改划"服务链路"(获客→激活→留存→付费→转介绍),两套切法都满足"每层一个维度"。

| 维度 | 归属层 | 说明 |
|------|--------|------|
| 例:跨线客户统一视图/OneID | 客户经营层(自研系统) | 唯一无可替代的数据 |
| 例:记账作业/账期/服务进度 | 作业执行层(代账系统) | 不做,集成调用 |
| 例:获客触达 | 现成(销氪等) | 不自研 |

## 四、现有系统覆盖(自研 vs 现成+增量)
| 功能诉求 | 现成系统是否已覆盖? | 覆盖判断的依据状态 | 缺口(现成+增量 还是 必须自研) |
|---------|-------------------|--------------------|------------------------------|
| | | [查证/听说的/ASSUMPTION] | |

> 覆盖判断是新列:**反例核心教训——"销氪能给多少"若只是听说的而非查证的,存在性闸等于没过**。每行标注依据状态,听说的须核实后才能作为"现成已覆盖"的结论。

## 五、价值主张
- 给一线:
- 给管理者:
- (若内部工具:重点在"值不值",不是"怎么赚")